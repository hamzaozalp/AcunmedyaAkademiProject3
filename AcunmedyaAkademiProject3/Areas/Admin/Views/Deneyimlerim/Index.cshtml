@model IEnumerable<AcunmedyaAkademiProject3.Entities.Experience>

@{
    ViewData["Title"] = "Deneyimlerim Yönetimi";
    Layout = "_AdminLayout";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Deneyimlerim Yönetimi</h2>
        <a asp-area="Admin" asp-controller="Deneyimlerim" asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Yeni Deneyim Ekle
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>İş Unvanı</th>
                            <th>Şirket Adı</th>
                            <th>Başlangıç Tarihi</th>
                            <th>Bitiş Tarihi</th>
                            <th>Açıklama</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="vertical-align: middle;">@item.Title</td>
                                <td style="vertical-align: middle;">@item.CompanyName</td>
                                <td style="vertical-align: middle;">@item.StartDate</td>
                                <td style="vertical-align: middle;">@item.EndDate</td>
                                <td style="vertical-align: middle;">@item.Description</td>
                                <td style="vertical-align: middle;">
                                    <div class="btn-group" role="group" aria-label="Deneyim İşlemleri">
                                        <a asp-area="Admin" asp-controller="Deneyimlerim" asp-action="Edit" asp-route-id="@item.ExperienceId" class="btn btn-sm btn-warning d-flex align-items-center">
                                            <i class="fas fa-edit me-1"></i> Düzenle
                                        </a>
                                        <form asp-area="Admin" asp-controller="Deneyimlerim" asp-action="Delete" asp-route-id="@item.ExperienceId" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger d-flex align-items-center" onclick="return confirm('Bu deneyimi silmek istediğinizden emin misiniz?')">
                                                <i class="fas fa-trash-alt me-1"></i> Sil
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @* Buraya özel scriptler eklenebilir *@
} 