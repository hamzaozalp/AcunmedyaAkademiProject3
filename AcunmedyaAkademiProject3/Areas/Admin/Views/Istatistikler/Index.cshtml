@model IEnumerable<AcunmedyaAkademiProject3.Entities.Statistic>

@{
    ViewData["Title"] = "İstatistikler Yönetimi";
    Layout = "_AdminLayout";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>İstatistikler Yönetimi</h2>
        <a asp-area="Admin" asp-controller="Istatistikler" asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Yeni İstatistik Ekle
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Başlık</th>
                            <th>Değer</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="vertical-align: middle;">@item.Title</td>
                                <td style="vertical-align: middle;">@item.Value</td>
                                <td style="vertical-align: middle;">
                                    <div class="btn-group" role="group" aria-label="İstatistik İşlemleri">
                                        <a asp-area="Admin" asp-controller="Istatistikler" asp-action="Edit" asp-route-id="@item.StatisticId" class="btn btn-sm btn-warning d-flex align-items-center">
                                            <i class="fas fa-edit me-1"></i> Düzenle
                                        </a>
                                        <form asp-area="Admin" asp-controller="Istatistikler" asp-action="Delete" asp-route-id="@item.StatisticId" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger d-flex align-items-center" onclick="return confirm('Bu istatistiği silmek istediğinizden emin misiniz?')">
                                                <i class="fas fa-trash-alt me-1"></i> Sil
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @* Buraya özel scriptler eklenebilir *@
} 